[build]
  command = "npm install && npm run build"
  publish = "dist/client"

[build.environment]
  NITRO_PRESET = "netlify"
  NODE_VERSION = "22"

[[edge_functions]]
  path = "/*"
  function = "server"
